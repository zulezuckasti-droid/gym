export default function WorkoutLoading() {
  return (
    <main className="mx-auto flex min-h-dvh w-full max-w-lg flex-col items-center justify-center px-4 pt-[max(1rem,var(--safe-area-top))] pb-[max(1rem,var(--safe-area-bottom))]">
      <p className="text-sm text-muted-foreground">Loading…</p>
    </main>
  );
}
